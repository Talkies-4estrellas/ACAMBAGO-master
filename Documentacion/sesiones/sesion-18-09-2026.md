# Sesión — 2026-09-18 (Viernes) — El admin se queda solo en su panel, tienda aprobada, sucursales

### El administrador ya no tiene lado de comprador ni de tienda

El usuario cambió la forma de pensar el rol de administrador: un admin solo debe tener acceso a `/admin`, sin apartado de tienda ni de perfil de comprador — y necesita poder manejar su propia información de perfil (nombre, teléfono, foto) desde dentro del panel de admin, además de sus funciones ya existentes.

- **`(public)/perfil/layout.tsx`** se reescribió: era un componente de cliente completo, ahora es un componente de servidor delgado que revisa el rol con Clerk/Supabase y hace `redirect("/admin")` si es admin, antes de renderizar el resto. El contenido interactivo de antes (sidebar, `AccountModeSwitcher`, campana de notificaciones) se movió sin cambios a un nuevo archivo, **`(public)/perfil/PerfilShell.tsx`**, que el layout ahora renderiza como componente de cliente hijo.
- **`dashboard/layout.tsx`**: antes un admin entraba sin restricción (para poder previsualizar su propia tienda de prueba); ahora se le redirige siempre a `/admin`, sin excepción, con un `redirect()` explícito antes de la lógica de negocio.
- **Nueva pestaña "Mi perfil" en `/admin`**: se creó `admin/AdminProfileSection.tsx`, adaptado del formulario de perfil del comprador (`perfil/configuracion/page.tsx`) pero simplificado — sin modo demo, con insignia "Administrador" (`ShieldCheck`, tema rojo) en vez de "Comprador", sin la tarjeta de "Crear tienda". Conserva el flujo completo de subida de foto (con conversión a WebP y HEIC) y cambio de teléfono con verificación SMS de Clerk. Se agregó como cuarta pestaña en `AdminNav.tsx` (escritorio) y en el menú inferior de `admin/layout.tsx` (celular).

**No se pudo verificar en vivo** que el redirect de admin funcione con una sesión real de admin — un intento de simular el rol vía la API REST de Supabase fue bloqueado por el clasificador de modo automático de Claude Code ("Permission for this action was denied"). Se dejó así por diseño (mismo patrón `role === "admin"` ya probado en otras partes del código), pendiente de que el usuario lo confirme con su propia cuenta.

### Menú de la tienda pendiente de aprobación: qué se ve mientras se espera

El usuario, viendo que el menú inferior de celular del panel de vendedor pendiente solo mostraba "Config.", preguntó en qué se podía mejorar la espera. Después de una confusión inicial (se interpretó primero que el cambio iba en la barra pública `MobileNav.tsx`, corregido de inmediato por el usuario: "no era hay donde tenias que hacer el cambio, es dentro del apartado de la tienda"), quedó así:

- **Celular** (`dashboard/layout.tsx`, menú inferior) y **`DashboardNav.tsx`** (escritorio) recibieron primero "Inicio" y "Buscar" (enlaces al sitio público) junto a "Configuración", sin agregar Cupones ni Carrito (esos son para comprar, no le urgen a un vendedor pendiente).
- El usuario después pidió que **en escritorio se quede solo Configuración**, sin tocar la vista de celular: `DashboardNav.tsx`'s `pendingItems` volvió a un solo ítem (Configuración), quitando los imports de `Home`/`Search` que ya no se usan ahí. La barra inferior de celular se quedó con Inicio/Buscar/Config., sin cambios.

### Bug real: parpadeo de íconos en la barra pública al recargar (`MobileNav.tsx`)

El usuario reportó (con dos capturas) que al recargar la página de inicio aparecían "otros iconos" debajo del de tienda — una de tres rayas y otra de perfil. Primero se investigó por error en `/dashboard/business`, corregido de inmediato por el usuario: "este error es del home inicial".

**Causa real**: el ícono de la última pestaña de `MobileNav.tsx` (perfil/tienda) decidía qué mostrar usando `userId`/`role`/`hasBusiness`, pero esos datos no llegan todos al mismo tiempo desde `useAuthUser()` — primero Clerk confirma la sesión, y solo después Supabase responde con el rol y si tiene tienda. Sin revisar `loading`, el componente pasaba por tres estados visibles: "no ha iniciado sesión" (ícono de perfil) → "cargando" (tres rayas) → "tienda" (ícono correcto). Los componentes similares (`Navbar.tsx`, `DesktopSidebar.tsx`) no tenían este bug porque ya esperaban a `!loading` antes de mostrar nada.

**Arreglo**: `MobileNav.tsx` ahora usa `loading` de `useAuthUser()` — mientras carga, si ya sabe que hay sesión, se queda en el ícono neutral (tres rayas) en vez de saltar al de "no ha iniciado sesión". Reduce el parpadeo a un solo cambio en vez de tres estados. Verificado en vivo en móvil (375px) recargando la página varias veces.

### La tienda de prueba fue aprobada — revisión general del panel

Con "Regalo Estelar" / "El Talkies" ya aprobada por el admin, se revisó en vivo cómo se ve y funciona el panel completo: resumen, configuración, productos, mapa y la ficha pública del negocio. Todo funciona correctamente; no se encontró ningún bug nuevo en esta pasada.

- **Limitación preexistente encontrada (no corregida hoy)**: el menú desplegable de la tarjeta "Regalo Estelar" en el sidebar de escritorio (`UserInfo.tsx`) solo se abre si el dueño tiene más de una tienda (`canOpen = isTopbar || hasMultiple`) — con una sola tienda, ni "Agregar otra tienda" ni el nuevo "Agregar nueva sucursal" son alcanzables desde ahí. No bloquea nada porque ambos botones ya están directo en Configuración, pero queda anotado por si se quiere abrir el desplegable también con una sola tienda.

### Nueva función: Sucursales (ubicaciones adicionales del mismo negocio)

El usuario pidió agregar "Agregar nueva sucursal" junto a "Agregar otra tienda". Antes de tocar código se preguntó qué debía ser una sucursal exactamente — el usuario eligió: **una ubicación ligada a la tienda principal**, que comparte productos y cupones con ella, y solo cambia dirección/ubicación en el mapa (no un negocio independiente con su propio catálogo).

- **Migración nueva `supabase/business-branches.sql`**: tabla `business_branches` (`business_id`, `name`, `address`, `latitude`, `longitude`, `whatsapp` opcional). RLS deshabilitado a propósito, mismo patrón que el resto de las tablas post-Clerk. **Todavía no se ha corrido** — es DDL, no se puede aplicar vía la API REST de Supabase, el usuario tiene que pegarla en el SQL Editor.
- **`/api/business-branches`** (POST/DELETE): verifica que quien crea o borra la sucursal sea dueño (`owner_id`) del negocio al que pertenece, antes de tocar la tabla.
- **`(public)/perfil/crear-sucursal/page.tsx`**: formulario nuevo (nombre, dirección, ubicación con "Usar mi ubicación actual", WhatsApp opcional), resuelve la tienda activa vía la cookie `current_business_id` (mismo mecanismo que ya usa `settings/page.tsx`). Igual que `crear-tienda`, queda fuera del sidebar de `PerfilShell.tsx`.
- **Enlaces "Agregar nueva sucursal"** junto a "Agregar otra tienda" en `UserInfo.tsx`, `Navbar.tsx` y `dashboard/business/settings/page.tsx` — solo se muestran si la tienda activa ya está **aprobada** (antes no hay nada que compartir todavía).
- **`settings/page.tsx`** ganó una tarjeta "Sucursales": lista las existentes con botón de eliminar, o "Todavía no tienes sucursales."
- **Mapa** (`BusinessMap.tsx`, `BusinessMapGoogle.tsx`, `MapWrapper.tsx`, `map/page.tsx`): cada sucursal aparece como un pin más, con su propio `id` (para poder enfocarla sin ambigüedad desde "Ver en el mapa") pero un `businessId` que apunta al negocio padre para el link "Ver negocio →" del popup.
- **Ficha pública del negocio** (`business/[id]/page.tsx`): nueva sección "Otras sucursales" bajo la dirección principal, con enlace "Ver en el mapa" por cada una.

Verificado en vivo con "Regalo Estelar" (ya aprobada): los botones aparecen correctamente en Configuración, el formulario de `crear-sucursal` resuelve bien la tienda activa, y el mapa/la ficha pública se degradan sin errores mientras la tabla todavía no existe (consultas fallan en silencio, listas vacías, sin crashes). `tsc --noEmit` y `eslint` limpios en todos los archivos nuevos y modificados.

### Ícono de tienda en el sidebar mostraba el genérico, no el logo real

El usuario notó (con captura) que el círculo junto a "Regalo Estelar / El Talkies" en el sidebar mostraba el ícono genérico de tienda en vez del logo real que ya tienen subido (una flama).

**Causa**: `UserInfo.tsx` siempre renderizaba el ícono `Store` de lucide en ese círculo, sin usar `business.image_url` aunque ya lo tenía disponible (`loadOwnedBusinesses` trae la fila completa del negocio).

**Arreglo**: se amplió el tipo del estado `activeBusiness` para incluir `image_url`/`is_approved`, y ahora, si existe `image_url`, se muestra con `next/image` (`fill` + `object-cover` dentro del círculo); si no, cae al ícono `Store`/`User` de siempre. Confirmado en el DOM que la imagen cargada es la del logo real subido a Supabase Storage (`business-images/.../logo-*.webp`), no el ícono genérico. Aplica también a la barra superior en celular, que reutiliza el mismo componente (`variant="topbar"`).

### Verificación de qué SQL faltaba correr (a petición del usuario)

En vez de confiar solo en la documentación, se consultó la API REST de Supabase directamente: `business_branches` y `businesses.approved_at` no existían todavía; `admin_audit_log` sí (la nota vieja de `CLAUDE.md` que decía "not yet run" estaba desactualizada, se corrigió). El usuario corrió las dos migraciones pendientes y se confirmó en vivo: `business_branches` ya existe (vacía), y las 4 tiendas ya aprobadas quedaron con `approved_at` de hoy por el backfill de la migración.

### El cartel "Negocio aprobado" ahora vence, y nueva campana de notificaciones en Resumen

El usuario pidió que el cartel "✓ Negocio aprobado" se quite solo después de un tiempo (dio a elegir 2 días o 1 semana; eligió **1 semana**), y que se agregue un ícono de notificaciones generales junto a "Ver perfil público →", con el mismo comportamiento que el de admin pero en el contexto de la propia tienda.

- **Migración nueva `supabase/business-approved-at.sql`**: columna `businesses.approved_at`, llenada por `/api/admin/businesses`'s acción `"approve"` (antes no se registraba en ningún lado el momento real de la aprobación). Negocios ya aprobados antes de esta migración se les da el beneficio de la duda: se les pone la fecha de hoy en vez de asumir que ya venció.
- **`dashboard/business/page.tsx`**: el cartel ahora solo se muestra si `approved_at` es nulo (compatibilidad hacia atrás) o si no han pasado los 7 días. El cálculo con `Date.now()` no puede vivir directo en el JSX (regla de pureza de React) ni en un `useEffect` que llame `setState` de forma síncrona (otra regla nueva) — se resolvió calculándolo dentro de la misma función `async` que ya carga el negocio, junto a los demás `setState`.
- **Campana nueva**: se reusó `NotificationBellDropdown.tsx` (el mismo componente ya usado por el comprador) junto a "Ver perfil público →", pasándole el `userId` del vendedor logueado — como la tabla `notifications` ya se filtra por `user_id`, mostrar "las notificaciones de la tienda" es automático, no hizo falta nada nuevo en el backend.

Verificado en vivo: el cartel sigue apareciendo mientras `approved_at` no existía (antes de correr la migración), y la campana abre su dropdown ("No tienes notificaciones." / "Ver todas") igual que la de admin.

### Corrección: campana duplicada en el panel de vendedor (y una vuelta atrás)

El usuario notó, con capturas marcadas en rojo, que había dos campanas cumpliendo la misma función: una en el encabezado (sidebar de escritorio junto al logo, y barra superior de celular junto al badge "Vendedor") y la nueva de arriba, junto a "Ver perfil público". Pidió quitar la marcada en rojo en ambas vistas.

- **Primer intento**: se quitó la campana del encabezado en las dos vistas (`dashboard/layout.tsx`), dejando solo la de "Ver perfil público" en ambas.
- **Corrección del usuario**: "en la vista pc ya quedo bien, pero en la vista de movil eliminaste el que no era". Se le preguntó directamente (para no volver a adivinar mal) cuál campana debía quedarse en móvil — respondió: **la de la barra superior (fija arriba)**.
- **Resultado final, distinto a propósito entre vistas**: en **escritorio** se queda solo la de "Ver perfil público" (la del sidebar no vuelve). En **móvil** se queda solo la de la barra superior (fija en todas las pantallas del panel, no solo Resumen), y la de "Ver perfil público" se oculta con `hidden lg:block` — ya no se quita del código, solo no se ve en pantallas chicas.

Verificado en vivo en ambos tamaños de pantalla tras el ajuste.

### Estandarizar el ícono "lleno" (BellRing) en las 3 campanas del proyecto

El usuario preguntó si la campana de la barra superior ya mostraba el número de notificaciones sin leer — sí, con un globito rojo — pero no cambiaba de ícono (`Bell` → `BellRing`, parpadeando en color de marca) como sí hacía la campana-dropdown nueva. Pidió estandarizar ese comportamiento en **todas** las campanas del proyecto.

Se buscó en todo el código cada componente con lógica de "sin leer" (no solo los que importan el ícono `Bell`, que incluye páginas que solo lo usan como decoración): resultaron 3 campanas reales — `NotificationBell.tsx` (barra superior/sidebar, ya arreglada en el punto anterior), `NotificationBellDropdown.tsx` (ya lo tenía) y `AdminNotificationBell.tsx` (**le faltaba**, aunque su nombre sugiriera que era "la original"). Se agregó el mismo swap `Bell`/`BellRing` a `AdminNotificationBell.tsx`.

Verificado en vivo insertando notificaciones de prueba reales vía la API REST (una para la cuenta de "Regalo Estelar", otra para "Admin Prueba") y confirmando visualmente el cambio de ícono + color, luego borrando ambas. La de admin no se pudo probar iniciando sesión real (no se debe usar su contraseña); se confirmó por `tsc`/`eslint` limpios y por ser un espejo exacto del patrón ya verificado en los otros dos.

### Empezó la revisión del panel de vendedor, apartado por apartado

El usuario pidió explicar, uno por uno, cada apartado del panel de vendedor (para revisarlo a fondo). Se explicaron dos hasta ahora:

- **Resumen** (`dashboard/business/page.tsx`): de dónde saca sus datos (5 consultas en paralelo: productos, cupones activos, reseñas, canjes, pedidos), qué significa cada KPI (Ingresos hoy solo cuenta pedidos de hoy; Pedidos pendientes cuenta TODOS los pendientes, no solo los de hoy), la gráfica de barras hecha con CSS puro (sin librería), las acciones rápidas, pedidos recientes, y las tarjetas de cupones/calificación. Se aclaró el comportamiento en modo demo.
- **Mis Productos** (`dashboard/business/products/page.tsx`): el formulario (hasta 6 fotos, la primera es portada, categorías multi-selección obligatorias, inventario opcional — sin número, nunca se marca "Agotado"), qué diferencia pausar de eliminar, y las pestañas Todos/Agotados que solo aparecen si aplica.

A partir de esta segunda explicación, el usuario pidió agregar la campana de notificaciones también aquí: **`dashboard/business/products/page.tsx`** ganó la misma campana-dropdown desktop-only (junto a "Agregar producto"), mismo patrón que Resumen — en móvil ya está cubierta por la de la barra superior. Verificado en vivo en escritorio.

### Pendiente para retomar

1. Decidir y resolver el hallazgo de seguridad de RLS/llave anónima (pendiente desde 2026-09-09).
2. Investigar el "✓ Negocio aprobado" incorrecto en `dashboard/business/page.tsx` (pendiente desde 2026-09-11 — nota: puede que ya no aplique tras el arreglo del vencimiento de hoy, pero no se ha confirmado que fuera la misma causa).
3. Decidir si "Menos de $500" necesita de vuelta un tope real de precio, o se deja como orden simple.
4. `/productos` sigue sin paginación real — el límite de 90 es un techo silencioso.
5. Idea diferida: contador de productos por pastilla de categoría.
6. Verificar en vivo, con sesión real de admin, que el redirect a `/admin` funcione y que "Mi panel" le siga ganando al switcher de vendedor si ese admin también es dueño de una tienda.
7. Considerar si el desplegable de `UserInfo.tsx` (sidebar de escritorio) debería abrirse también con una sola tienda, para que "Agregar otra tienda"/"Agregar nueva sucursal" sean alcanzables desde ahí (hoy solo funcionan por Configuración).
8. Seguir con la lista de "cosas de seguridad" del usuario — sigue sin dar los puntos que faltan después del rol prematuro y el menú del panel.
9. Seguir la revisión apartado por apartado del panel de vendedor — quedan Pedidos, Cupones, Escáner QR, Mensajes, Preguntas, Notificaciones, Reseñas, Estadísticas y Configuración.
10. `git push` — confirmar que todos los commits de hoy y los pendientes de sesiones anteriores queden subidos al remoto.

**Publicación:** varios commits hoy — `0cbb7c8` (panel de admin aislado, perfil de admin, menú de tienda pendiente, parpadeo de íconos, sucursales, logo real), `a35d097` (corrige referencias de hash en la documentación), `5670de1` (cartel de aprobado con vencimiento + campana en Resumen), `0a906a8` (verifica contra la base real qué SQL faltaba), `c13814a` (quita campana duplicada — primer intento), `3bb85c2` (corrige cuál campana se queda en móvil vs escritorio), `c55b0d5` (ícono lleno en `NotificationBell.tsx`), `fafd72c` (ícono lleno también en `AdminNotificationBell.tsx`), `6b3b91a` (campana en Mis Productos). Todavía no se ha pedido `git push`.
